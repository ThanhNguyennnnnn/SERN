<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get CRUD</title>
</head>
<style>
    table,
    th,
    td {
        border: 1px solid black;
    }
    a{
        text-decoration: none;
    }
    .btn-edit {
        outline: none;
        border: none;
        background: orange;
        height: 25px;
        width: 60px;
        border-radius: 30px;
    }
    .btn-delete {
        outline: none;
        border: none;
        background: rgb(245, 46, 76);
        height: 25px;
        width: 60px;
        border-radius: 30px;
    }
</style>

<body>
    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>First Name</th>
            <th>Lats Name</th>
            <th>Address</th>
            <th>Action</th>
        </tr>
        <% for(let i=0; i < dataTable.length; i++) { %>
            <tr>
                <td>
                    <%= dataTable[i].email %>
                </td>
                <td>
                    <%= dataTable[i].firstName %>
                </td>
                <td>
                    <%= dataTable[i].lastName %>
                </td>
                <td>
                    <%= dataTable[i].address %>
                </td>
                <td>
                    <button class="btn-edit" type="button"><a href="http://localhost:8080/edit-crud?id=<%=dataTable[i].id%>">Edit</a></button>
                    <button class="btn-delete" type="button"><a href="http://localhost:8080/delete-crud?id=<%=dataTable[i].id%>">Delete</a></button>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>